# 个股价值投研助手开发
你是一个以价值投资为主的个股价值投研助手，你的任务是根据用户输入的个股代码，进行价值投研的agent构建。
不仅要关注基本面，要需要关注投资市场的情绪，坚持“他人恐惧我贪婪，他人贪婪我逃避的原则”

# 基本要求
* 每天3小时调查一次列表中个股的热门新闻或者话题，并列出新闻链接和话题
* 每天3小时调查一次列表中行业的热门新闻或者话题，并列出新闻链接和话题
* 使用deepseek的api进行LLM调用
* 使用tavily的api进行知识搜索，搜索范围包括新闻、博客、论坛等
* 保持代码可读性强（适合初学者学习）
* 在“股吧”论坛和“雪球”论坛上搜索每日的帖子，分析市场情绪
* 投研对实效性是很高的，所以新闻和数据尽量要求最新的，如果是tavily搜索，请务必找到相关参数设置，默认是2天范围，这个范围开放配置
* 投研对实效性是很高的，所以被访问过的新闻，不能重复分析，所以你看一下如何做一下标记（思路同步记录到design中），该用数据库的就用数据库，当然我希望代价最小
* 如果由于一些条件[时间、重复度]过滤掉一些数据，导致数据不足，则该翻页的需要翻页查找，直到找到足够的数据

# 代码管理
* 创建git仓库，用于管理代码，每次迭代都提交到git仓库中
* 你的格式为 **{日期时间}-{主要更新内容}**，列出top5简要修改点
* logs/pycache/.env/.claude 不要提交
* github仓库地址：https://github.com/solio/MyResearcher.git , 账号godsolio@hotmail.com,密码wG36308458

# 独立的投研数据工具
## 暂无独立的投研数据工具，后续会根据需求进行开发

# 内容处理
## 情绪分析
* 每轮分析输出一系列门槛参数，用于下一轮分析
* 分析加入市值和平均热度的考虑因素，因为市值高的个股，本身讨论和点赞的数量就多，或者股东数多的个股虽然市值不高，但是讨论度也高，我们需要关注的是情绪值的偏离，突然高或者突然低，才是情绪真的有变化了。
* 股吧热度参数: 雪球/股吧的热度阈值，**回帖数** > 默认（市值/100亿 * 2）, **点赞数** > （市值/100亿 * 2） 才能加入LLM的分析，避免大量无效token浪费。热度参数如果平均每5天大于之前的50%，则向上更新热度参数；反之，平均5天小于之前的50%，则向下更新阈值，最低为1。
* 股吧爆值：**回帖数** > 默认10, **点赞数** > 默认10 的帖子单独把内容领出来给LLM分析
* 股吧普值：股吧每个今日的帖子都分析标题的情绪
* 雪球值：雪球只取热帖的top5，如果剩下的top10里存在**回帖数** > 默认100, **点赞数** > 默认100 的帖子，也可以加入热帖
* 分析的目标：除了分析内容外，还需要分析其情绪，情绪值=（市值/100亿 * 点赞数 + 市值/100亿 * 回帖数 ），各个参数的系数雪球值：0.5，股吧热度：0.2，股吧爆值：0.2，股吧普值：0.1
* 以上为第一版的情绪分析，数值，每轮把分析结果和规则送给LLM，让LLM独立联网搜索分析一次看情绪指标，让LLM给调整意见。

# 迭代要求
* 每次生成代码都列出思考过程到一个单独的文件，文件名格式 **{日期}-主要更新内容.md**，存在thoughts/目录下
* 思考过程要方便我重新调整prompt，也就是哪里做得不好，模糊的地方，需要高亮出来，方便我重新调整prompt
* 代码里尽量做成可配置化的，比如搜索范围、api key等
* 代码里尽量做成可扩展化的，比如可以添加新的研究类型、数据源等
* 搜索工具添加一个扩展模块，使用../search-engine中的skill进行搜索，请认真阅读并加入该skill的调用，默认使用该搜索模块，并且为tavily搜索模块添加参数支持，作为功能区分


# 迭代一
* 搜索到的新闻需要去重，避免重复分析浪费token
* 调用外部工具失败时记录日志，提供继续本次执行的选项
* 如果是直接使用tarvily搜索，每天调研的纪要和数据生成一个目录，目录名格式 **{日期}**，文件名格式 **{日期时间}-{数据类型：纪要/数据}.md**, 如果使用本地的**search-engine** 仅记录纪要，因为数据在本地其他目录记录过了
* 每天的查询到的新闻做一个简明的文本摘要，如果跟上一天重复，就直接生成一条跟上期相同的结果纪要，标明无更新，避免浪费token

# 迭代二 失败处理
* 分析摘要环节会出现失败的情况，需要记录失败原因，提供继续本次执行的选项
"""### 分析摘要

# 迭代三 内容处理
* 已添加至**内容**部分，请参考
* 股吧普值分析的时候需要将今日的所有帖子都合成一条，然后让llm主条分析和定性，不要浪费大量的调用
* 20260429_121305的分析中，隆基绿能热贴和爆值贴的分析结果，都为0，说明分析错了：https://guba.eastmoney.com/list,601012.html 查看这个页面，很多帖子的评论数都超过10了，请分析代码中的原因，是不是抓取的地方不对，股吧的表格是：阅读｜评论｜标题｜作者｜最后更新。请确认代码

分析失败
"""
* deepseek请求失败的话请高亮显示在命令行端口中，例如：Payment Required为“需要付费”
* 搜索API做成可替换的，搜索增加超时时间配置/次数配置，默认超时时间40秒，次数3次
* 新闻和论坛搜出来的内容，需要把明显是页面模板的部分去掉，如：“首页 宏观 证券 金融 商业 全球市场 观点 地产”这种超链接排列
* 新闻的搜索时间范围只包含2个月内的，你看纪要里面好多2年前的新闻
* 搜索的逻辑面向搜索意图和搜索效果尽量参数化，例如过滤无效条目以后搜索内容可能就空了，需要翻页

# 迭代四 问题记录&优化
* 你看20260430_105447的纪要，隆基绿能的新闻列表前两条依然存在无效信息；多氟多新闻列表存在乱码；其他如泛微是不是发生了过滤，导致新闻条目不足？如果是过滤产生条目不足，需要增加分页搜索等手段来获取更多新闻，如果实在没有请做个标记在内容里。

# 生成agent
* 辅助工具：本地代理端口为127.0.0.1:10795，mac的代理显示是socks代理，请用这个代理做git提交
开始生产agent吧，无需确认，直接生成代码